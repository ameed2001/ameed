'use client';

// This file is intentionally left blank to disable the previous JavaScript-based layout shift fix,
// which was causing conflicts with the component library's built-in behavior.
// A new, simpler CSS-only solution has been implemented in globals.css.
