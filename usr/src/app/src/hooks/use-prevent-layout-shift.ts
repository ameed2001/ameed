
'use client';

// This hook is no longer needed and has been disabled to prevent layout shifts.
// The dropdown menu's modal behavior is now controlled directly.
const usePreventLayoutShift = (isOpen: boolean) => {
  // No-op
};

export default usePreventLayoutShift;
